# ============================================================================
# CLOUD DEPLOYMENT ENVIRONMENT (.env.cloud)
# Copy and customize for your cloud provider (GCP/AWS/Azure)
# ============================================================================

NODE_ENV=production
DATABASE_TYPE=postgresql

# ============================================================================
# DATABASE CONFIGURATION - Choose your cloud provider
# ============================================================================

# Google Cloud Platform (GCP) Cloud SQL
# DATABASE_URL=postgresql://username:password@/database_name?host=/cloudsql/project-id:region:instance-name

# Amazon Web Services (AWS) RDS
# DATABASE_URL=postgresql://username:password@your-db-instance.region.rds.amazonaws.com:5432/database_name

# Microsoft Azure Database for PostgreSQL
# DATABASE_URL=postgresql://username:password@your-server.postgres.database.azure.com:5432/database_name?sslmode=require

# Generic PostgreSQL (any cloud provider)
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# ============================================================================
# SERVICE CONFIGURATION
# ============================================================================

# Cloud service ports (usually set by cloud provider)
PORT=${PORT:-8080}
API_GATEWAY_PORT=${PORT:-8080}

# Cloud URLs (replace with your actual deployed URLs)
REACT_APP_API_URL=https://your-api-gateway-url
REACT_APP_API_GATEWAY_URL=https://your-api-gateway-url

# ============================================================================
# SECURITY CONFIGURATION (PRODUCTION)
# ============================================================================

# Generate strong secrets for production
JWT_SECRET=your-super-strong-jwt-secret-here
JWT_EXPIRY=1h
REFRESH_TOKEN_EXPIRY=7d
SESSION_SECRET=your-super-strong-session-secret-here

# Production Security
LOG_LEVEL=info
CORS_ORIGIN=https://your-frontend-domain.com
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# ============================================================================
# FEATURE FLAGS (PRODUCTION)
# ============================================================================

ENABLE_AI_PREDICTIONS=true
ENABLE_ZOHO_SYNC=true
ENABLE_CALL_PRIORITIZATION=true
ENABLE_PERFORMANCE_TRACKING=true

# ============================================================================
# INTEGRATIONS (PRODUCTION)
# ============================================================================

# Zoho CRM (Production)
ZOHO_CLIENT_ID=your-zoho-client-id
ZOHO_CLIENT_SECRET=your-zoho-client-secret
ZOHO_REFRESH_TOKEN=your-zoho-refresh-token
ZOHO_REDIRECT_URI=https://your-api-url/auth/callback
ZOHO_SCOPE=ZohoCRM.modules.ALL,ZohoCRM.settings.ALL
ZOHO_SYNC_INTERVAL=3600000
ZOHO_BATCH_SIZE=100

# ============================================================================
# CLOUD-SPECIFIC CONFIGURATION
# ============================================================================

# Google Cloud Platform
# GOOGLE_CLOUD_PROJECT=your-project-id
# GCLOUD_PROJECT=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# AWS Configuration
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# Azure Configuration
# AZURE_SUBSCRIPTION_ID=your-subscription-id
# AZURE_RESOURCE_GROUP=your-resource-group